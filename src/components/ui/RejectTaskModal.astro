---
// components/ui/ModalAddNew.astro
const { modalId = "deleteModal", title = "Reject Task" } = Astro.props;
import { CloseCircleIcon } from "../Icons";
---

<div class="modal fade" id={modalId} tabindex="-1">
  <div class="modal-dialog">
    <div class="modal-content p-5">
      <!-- Modal Header -->
      <div class="modal-header border-0 d-flex justify-content-end">
        <img
          role="button"
          data-bs-dismiss="modal"
          src="images/png/icons/close-circle-1.svg"
          alt="close"
        />
      </div>

      <!-- Modal body -->
      <div class="modal-body text-center py-2 border-0">
        <img src="images/png/icons/warning.svg" alt="warning" />
        <p class="fs-22 fw-bold mt-3">{title}</p>
        <p>Are you sure you want to reject task?</p>

        <div class="form-floating">
          <textarea class="form-input" rows="2" id="comment"></textarea>
          <label for="comment">Comment</label>
        </div>
      </div>

      <!-- Modal footer -->
      <div class="modal-footer d-flex justify-content-center border-0">
        <button
          type="button"
          class="btn text-white"
          style="background-color: red;"
          data-bs-dismiss="modal"
        >
          Yes, Reject</button
        >
      </div>
    </div>
  </div>
</div>
